import React from 'react';
import ReactDOM from 'react-dom';

const intro = (
<div>
    <h1>Hello World</h1>
    <p>Just a quick test for the basics of JSX and
        the react library.
    </p>
</div>
);

ReactDOM.render(intro, document.getElementById('root'));